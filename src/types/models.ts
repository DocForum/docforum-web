// Frontend-side TypeScript types mirroring backend/prisma/schema.prisma.
// TODO(Phase 1): keep in sync with schema.prisma once real fields exist.
// Do not let this drift silently — see AGENTS.md doc-update table.
export {};
